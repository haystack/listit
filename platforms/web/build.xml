<project>
    <import file="../../platform-common.xml"/>

    <target name="web.release" extensionOf="build.release">
        <zip destfile="${deploy.dir}/listit-${listit.version}.zip" basedir="${build.dir}" />
    </target>

    <target name="web.debug" extensionOf="build.debug" />
</project>


